# Extract concepts

Read only job `transcript.json`. Write stable readable IDs into `concepts.json`. Include only examinable claims grounded in cited contiguous segment IDs. Prefer rules, priorities, prohibitions, obligations, exceptions, figures, and practical cases. Do not add external facts.
